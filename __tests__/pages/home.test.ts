import { describe, it } from "node:test";


describe("Home page", () => {
    it.todo("Renders loading message during page loading"); 
    it.todo("Renders welcome message after page loading");
    it.todo("Renders not assigned to team message");
    it.todo("Renders social worker buttons when use is a social worker");
    it.todo("Renders manager buttons when user is a manager"); 
    it.todo("Renders administrator buttons when user is an admin");
    it.todo("Renders permission group not found message");
})